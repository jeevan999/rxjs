const express = require('express');
const routes = express.Router();


routes.delete('/food-items',(err,req,res)=>{
})

routes.delete('/servers',(err,req,res)=>{
})

routes.delete('/chairs',(err,req,res)=>{
})

module.exports = routes